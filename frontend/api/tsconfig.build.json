{
    "extends": "../../tsconfig.build.json",

    "compilerOptions": {
        "module": "CommonJS",
        "outDir": "./build",
        "types": ["node"]
    },

    "exclude": ["node_modules", "build"]
}
